# 安全策略

## 报告安全问题

不要在公开 Issue、Pull Request、博客或聊天记录中披露密码、Token、Cookie、密钥、证书私钥、生产数据或完整利用细节。

优先使用 GitHub 仓库提供的 Security Advisory 私下报告入口。如果该入口当前不可用，请联系 `yangxj96@gmail.com`，邮件标题使用 `Spectra Security Report`，并按照[安全问题报告](docs/参与贡献/03-安全问题报告.md)提供脱敏后的复现信息。

## 支持范围

安全报告可以涉及根工作区文档和脚本、`spectra-admin`、`spectra-ui` 以及 `logicflow-plugin-flowable`。第三方依赖自身的漏洞应同时参考其官方安全公告和版本要求。

报告时请注明受影响的仓库、子项目、提交或版本，以及漏洞需要的权限、部署条件和影响范围。没有在当前仓库实现或无法复现的内容，也请明确标注。

## 运行安全边界

- 生产环境必须使用受控的 HTTPS、精确的 Origin、正确的安全 Cookie 和 CSRF 策略。
- 安全 Redis 是 Token、Session、验证码、防重放和登录失败锁定的事实源；不可用时服务端保持 fail-closed。
- 本机配置、数据库密码、Redis 密码、S3 密钥、SSL 密码、安全主密钥和证书私钥不能提交到 Git。
- 文件上传、预览和下载必须遵守文件类型、业务引用和权限检查。

具体配置和生产操作见[后端配置说明](docs/后端/40-配置说明/00-后端配置说明.md)和[部署运维文档](docs/部署运维/00-容器部署.md)。
