---
tags:
  - guide
  - reference
---

# 常见命令

> 本页只保存可复用命令。首次配置、环境变量分级和数据库初始化见 [[10-环境搭建]]。

## 命令执行约定

- 当前开发环境统一使用 Ubuntu/WSL Bash；所有路径使用仓库相对路径或 POSIX 路径。
- 后端使用 `spectra-admin/mvnw`，不要依赖全局 Maven；Node 项目使用各自 mise 固定的 Node/pnpm。
- `.mise.local.toml`、`.env.development`、证书和 Provider 凭据是本机配置，必须先从 `.example` 创建。
- `pnpm install` 只在首次克隆或锁文件变化时执行，不是每次启动步骤。
- Docker 命令只属于部署参考，不属于当前 WSL 日常开发流程。

## 快速检查当前事实

从仓库根目录执行：

```bash
./scripts/agent-context.sh --facts
./scripts/check-docs.sh
```

以下检查是可选质量门禁，不参与 mise 激活或项目启动：

```bash
./scripts/check-agent-context.sh
./scripts/check-module-boundaries.sh
```

## spectra-admin

以下命令从 `spectra-admin/` 执行：

```bash
# 功能开发中按需快速反馈（以下以 spectra-core 为例）
mise exec -- ./mvnw -pl spectra-core -am compile
mise exec -- ./mvnw -pl spectra-core -am test

# 模块完成时：整理格式并验证当前模块及其依赖
mise exec -- ./mvnw -pl spectra-core -am spotless:apply
mise exec -- ./mvnw verify -pl spectra-core -am

# 全量格式检查
mise exec -- ./mvnw spotless:check '-Dspotless.ratchetFrom=NONE'

# 提交代码前：全项目整理并执行统一质量门禁
mise exec -- ./mvnw spotless:apply
mise exec -- ./mvnw verify

# 测试、编译、打包
mise exec -- ./mvnw test
mise exec -- ./mvnw clean compile
mise exec -- ./mvnw clean package -DskipTests

# 只构建指定模块及其依赖
mise exec -- ./mvnw clean compile -pl spectra-core -am
```

### 测试分类与外部依赖测试

默认的 `mise exec -- ./mvnw test` 只运行快速单元/契约测试，并排除 `integration` 与
`manual-integration` 标签；不会启动 Docker，也不会连接本机 PostgreSQL 或 Redis。

使用 Testcontainers 的可重复集成测试需要 Docker Runtime；当前 WSL 开发环境禁止执行：

```bash
mise exec -- ./mvnw -Pintegration -Dgroups=integration test
```

`-Dgroups=integration` 也会自动激活同名 profile。`manual-integration` 仅允许在明确隔离的
数据库上手工执行，不属于默认测试流程：

```bash
mise exec -- ./mvnw -Pmanual-integration -Dgroups=manual-integration test
```

### 启动已打包 JAR

```bash
jar=$(find spectra-launch/target -maxdepth 1 -type f \
  -name 'spectra-launch-*.jar' ! -name '*.jar.original' -print -quit)
test -n "$jar"
mise exec -- java --add-modules ALL-SYSTEM --enable-native-access=ALL-UNNAMED \
  -Dspring.profiles.active=dev \
  -jar "$jar"
```

模板默认使用 HTTPS：`https://127.0.0.1:4004/api/actuator/health`。

## spectra-ui

以下命令从 `spectra-ui/` 执行：

```bash
mise exec -- pnpm install
mise exec -- pnpm start
mise exec -- pnpm run format:check
mise exec -- pnpm run lint
mise exec -- pnpm run type-check
mise exec -- pnpm run test
mise exec -- pnpm run build
mise exec -- pnpm run preview
```

`pnpm start` 的 `prestart` 会执行会写回文件的 `format`、`lint:fix` 和类型检查。CI 或只读审查应使用
`format:check → lint → type-check → test → build`。

## logicflow-plugin-flowable

以下命令从 `logicflow-plugin-flowable/` 执行：

```bash
mise exec -- pnpm install
mise exec -- pnpm run format:check
mise exec -- pnpm run build
mise exec -- pnpm run dev
```

修改插件源码并与 Web 联调时，保持 `pnpm run dev` 监听，再在另一个终端启动 `spectra-ui`。

## 一次检查三个工程

从仓库根目录分项目执行，避免混淆工作目录：

```bash
(cd spectra-admin && mise exec -- ./mvnw spotless:check && mise exec -- ./mvnw test)
(cd logicflow-plugin-flowable && mise exec -- pnpm run format:check && mise exec -- pnpm run build)
(cd spectra-ui && mise exec -- pnpm run format:check && mise exec -- pnpm run lint \
  && mise exec -- pnpm run type-check && mise exec -- pnpm run test && mise exec -- pnpm run build)
```

## 停止开发服务

优先在对应启动终端按 `Ctrl+C`。只有确认端口确实由本次开发服务占用后，才终止对应 PID：

```bash
ss -ltnp | rg ':4004|:5173'
```

确认 PID 属于本次开发服务后，再使用 `kill -TERM <pid>`，不要终止不属于本项目的进程。

## CodeGraph（可选）

CodeGraph CLI 已安装并加入 PATH 时：

```bash
codegraph explore "要查询的符号或问题"
```

未安装时跳过；源码构建、测试和项目启动都不依赖 CodeGraph。

## VS Code

VS Code 任务和启动配置通过 PATH 中的 `mise` 使用项目固定工具链。如果找不到 `mise`，修复 VS Code
进程的 PATH 后重开窗口；不要把个人安装绝对路径提交进 `.vscode`。

## Git

提交、暂存、恢复和推送遵循项目 `spectra:git-execution-spec`。不要提交 `.mise.local.toml`、`.env*`
本机覆盖、证书、私钥、Token、密码、`.maven-repository/`、`.pnpm-store/` 或构建产物。

## 相关笔记

- [[00-项目总览]]
- [[10-环境搭建]]
- [[20-脚本工具]]
- [[10-架构分层]]
