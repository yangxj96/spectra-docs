---
tags:
  - backend
  - architecture
  - baseline
created: 2026-08-31
---

# 01 号任务：模块边界清单

> 本清单是 1.0.0 基线的架构盘点与实施记录，不改变 API、数据库结构或业务行为。盘点对象为 `spectra-admin` 后端，系统按单租户、非 SaaS 运行。安全能力已完成从独立 starter 模块向 `common`、`framework` 和 `core` 的合并。

## 使用规则

- 当前路径是事实证据，不直接等同于最终归属；最终归属以 [[02-能力归属矩阵]] 为准。
- 本清单记录模块、包、依赖、配置和重复能力；安全模块合并已完成，其他未完成项仍按对应批次推进。
- `spectra-launch` 是组合根；`spectra-core` 是必选系统核心，并内置统一通知与文件上传；OA、Workflow 是可选业务能力，未来 ERP 也按同一方式接入。
- 技术基础设施不因为 core 必选就全部放进 core：Web、Redis、MyBatis、加密和安全技术实现保留在 framework；core 只拥有系统必需的业务编排和领域能力。
- Flowable schema 中的 `tenant_id_` 等字段是引擎兼容字段，本清单不把它们当成应用租户能力。

## Maven 模块树

```text
spectra-admin
├── spectra-config                         配置资源，无 Java 主代码
├── spectra-common                         纯公共类型与跨模块契约、安全端口
├── spectra-framework                      Web/MVC/Redis/MyBatis/加密等技术基础设施
├── spectra-launch                         Spring Boot 组合根
└── spectra-modules
    ├── spectra-core                       必选系统核心（含通知、文件上传）
    ├── spectra-workflow                   可选流程引擎
    └── spectra-oa                         可选 OA
```

Maven 聚合关系为：根项目聚合 `spectra-config`、`spectra-common`、`spectra-framework`、`spectra-launch` 和 `spectra-modules`；`spectra-modules` 聚合五个业务模块。安全能力随 `spectra-common`、`spectra-framework` 和 `spectra-core` 发布，不再单独聚合或发布 starter。

## 模块职责与当前状态

| 模块 | 当前职责和包根 | 主/测试 Java | 角色索引（主代码） | 目标归属 | 当前结论 |
|---|---|---:|---|---|---|
| `spectra-config` | `src/main/resources`，无 Java 主代码 | 0 / 0 | 配置资源 | 配置资源层 | 保留；不能承载业务 Bean |
| `spectra-common` | `com.devops00.spectra.common` | 125 / 15 | 1 Mapper、3 Service | 公共类型、值对象、安全端口、错误码、上下文、审计/健康协议 | 不得反向依赖业务模块；生产源码不依赖 Spring/Web/数据库实现 |
| `spectra-framework` | `com.devops00.spectra.framework` | 103 / 47 | 1 Entity、1 Mapper、8 Service、13 Configuration；由 `FrameworkModule` 根包扫描发现 | 技术基础设施、安全配置、统一健康 Actuator 适配、持久化基类与 Web 响应适配 | 保留；不得依赖业务模块 |
| `spectra-launch` | `com.devops00.spectra.launch`、`architecture` | 3 / 13 | 启动类、模块装配、架构测试 | 组合根和架构门禁 | 保留；只做装配，不放业务实现 |
| `spectra-core` | `com.devops00.spectra.core` | 676 / 160 | 37 Controller、13 持久化 Entity、53 Mapper、126 Service、9 Configuration、3 HealthIndicator | 用户/认证/权限/组织/系统/调度/监控/审计/操作日志、通知和文件上传编排；系统配置枚举和写入端口归 `core.system` | 必选核心；通知与文件上传实现随 Core 提供，安全认证、授权、变更治理和安全审计编排均归 core，继续遵守不得依赖可选业务 |
| `spectra-workflow` | `com.devops00.spectra.workflow` | 42 / 2 | 4 Controller、2 Entity、2 Mapper、10 Service、1 Configuration、1 HealthContributor | Flowable 流程、任务、审批回调 | 可选业务；Flowable 内部兼容字段不外溢 |
| `spectra-oa` | `com.devops00.spectra.oa` | 216 / 5 | 14 Controller、32 Mapper、26 Service | 申请、请假、协同、报销、采购、资产、考勤、日历、通讯录、合同、文档、会议、公告、报表 | 可选业务；依赖 core 的公共能力和显式端口 |

角色统计通过 `scripts/check-module-boundaries.sh` 生成。统计按文件名后缀识别，不能代替对 Bean、包职责和调用链的语义审查；截至 Task 25，主代码共 1165 个 Java 文件，测试代码共 248 个 Java 文件。`@TableName` 实体共 79 个，API 端点文档按 54 个 REST Controller 维护。

## 当前直接依赖证据

下表只列与边界判断直接相关的依赖；完整 Maven 依赖以各模块 `pom.xml` 和检查脚本输出为准。

| 当前来源 | 当前直接依赖 | 对目标边界的判断 |
|---|---|---|
| `spectra-common` | Jakarta Validation API、JSpecify、SLF4J API、Lombok（测试另有 Spring Boot Test） | 生产源码只使用基础 API、注解和日志门面；Spring Web、MyBatis-Plus、PostgreSQL 实现已迁移到 framework |
| `spectra-framework` | common、Redis/Cache/Actuator、MVC、Spring Security、MyBatis-Plus、PostgreSQL、加密、验证码、MapStruct 等 | 技术层统一承载安全自动配置、过滤器、安全 Redis、持久化基类/Handler 和 Web 响应适配；不反向引用 core 业务 |
| `spectra-core` | common、framework、Spring Mail、AWS SDK S3、Apache Tika、IP2Region、Mica、Flyway | 通知与文件上传实现直接归 Core；不依赖 OA、Workflow 或其他可选业务实现；认证、授权、审计和操作日志编排均在 core |
| `spectra-oa` | core、workflow、POI | 作为可选业务可以依赖 core 和显式模块端口；文件能力通过 common.port.file 使用，避免 core 反向依赖 OA |
| `spectra-workflow` | Flowable、Mica XSS | 模块专属技术依赖，保留在 workflow，不下沉到 core |
| `spectra-launch` | core、OA、Workflow、Flyway | 是组合根，直接装配必选和可选模块属于预期职责；安全自动配置随 framework 入口装配 |

## 包域盘点

| 模块 | 当前一级包域 | 盘点结论 |
|---|---|---|
| common | `annotation`、`base`、`constant`、`dto`、`event`、`exception`、`foundation`、`port`、`scheduler`、`security` 等 | `foundation` 只承载纯 JDK 基础能力，`security.crypto` 只保留算法实现；事件、端口和值对象不继承 Spring 或绑定 ORM 实现 |
| framework | `web`、`properties`、`security`、`serialization`、`captcha`、`cache`、`persistence`、`health`、`assembler` | 技术基础设施统一承载 Web 响应、运行参数、ORM 基类/分页/Handler 和各自配置；不能持有业务 Entity/Mapper/Service |
| framework security | `framework.security` | Spring Security 运行机制、过滤器、Session/Token/验证码 Redis 适配归 framework；安全用户、认证用例、授权和安全审计领域归 core；公共端口归 common |
| core | `audit`、`common`、`scheduler`、`security`、`system`、`user` | `audit`、`security`、`user`、`system`、`scheduler` 是 core 必选能力；`common` 子包需拆分重复基础设施 |
| core.notification | `configuration`、`controller`、`dispatch`、`health`、`javabean`、`mapper`、`observability`、`policy`、`provider`、`security`、`sender`、`service`、`strategy` 等 | Core 内置通知领域闭环；脱敏和摘要/载荷加密适配归通知 feature；通过 common.notification 向外提供稳定端口 |
| core.upload | `api`、`configure`、`controller`、`health`、`javabean`、`mapper`、`properties`、`service`、`storage`、`validation` | Core 内置文件能力；通过 common.port.file 和 common.port.audit 提供跨域端口，健康检查实现统一协议 |
| workflow | `configuration`、`controller`、`javabean`、`mapper`、`service` | 流程引擎领域保持独立；OA 通过 workflow 端口交互 |
| OA | `application`、`asset`、`calendar`、`contact`、`contract`、`document`、`file`、`leave`、`meeting`、`notice`、`purchase`、`reimbursement`、`report`、`supply`、`workbench` | 全部属于可选 OA；文件引用归 `file.reference`，流程辅助归 `application.workflow`，对 core 的用户/组织/调度引用需通过稳定公共契约收敛 |

### Task 11 framework 包布局迁移评审登记

以下清单覆盖本次从 `framework.configure` 深层子包迁移的 64 个生产类。最终不保留 `framework.configure` 生产包，配置入口与运行时实现按能力归入 `web`、`security`、`serialization`、`captcha`、`cache`、`persistence`、`assembler` 和 `health`，各能力再按职责控制直接文件数量。迁移只改变 Java FQCN 和组件扫描边界，不改变 Bean 名称、配置前缀、Mapper namespace、REST 路径、Redis key 或序列化格式；统一回滚点为 Task 11 的后端隔离提交。

| 原包 | 类 | 目标包 | 原因 | 风险 / 回滚点 |
|---|---|---|---|---|
| `framework.configure.cache` | `CacheConfiguration` | `framework.cache.configuration` | 缓存配置集中于缓存配置子包 | 自动配置引用遗漏；回滚 Task 11 后端提交 |
| `framework.configure.cache` | `StandardCacheKeyGenerator` | `framework.cache` | 缓存运行时实现归位 | 缓存组件扫描遗漏；回滚 Task 11 后端提交 |
| `framework.configure.json` | `JacksonConfiguration` | `framework.serialization.jackson` | Jackson 配置归入序列化子包 | JSON Bean 未装配；回滚 Task 11 后端提交 |
| `framework.configure.json.properties` | `JacksonProperties` | `framework.serialization.jackson` | Jackson 属性与配置同包 | 配置绑定 FQCN 遗漏；回滚 Task 11 后端提交 |
| `framework.configure.kaptcha` | `KaptchaConfiguration` | `framework.captcha.configuration` | 验证码配置集中于配置子包 | 验证码 Bean 未装配；回滚 Task 11 后端提交 |
| `framework.configure.kaptcha.creator` | `KaptchaTextCreator` | `framework.captcha.generator` | 验证码生成实现归位 | 组件扫描遗漏；回滚 Task 11 后端提交 |
| `framework.configure.kaptcha.enums` | `KaptchaType` | `framework.captcha.generator` | 验证码生成类型归位 | 属性类型引用遗漏；回滚 Task 11 后端提交 |
| `framework.configure.kaptcha.properties` | `KaptchaProperties` | `framework.captcha.configuration` | 验证码属性与配置同包 | 配置绑定 FQCN 遗漏；回滚 Task 11 后端提交 |
| `framework.configure.mapstruct` | `GlobalMapperConfig` | `framework.serialization.mapper` | 映射装配契约归入 mapper 子包 | MapStruct 编译引用遗漏；回滚 Task 11 后端提交 |
| `framework.configure.mapstruct` | `TimeMapper` | `framework.serialization.mapper` | 时间映射运行时归位 | 映射组件扫描遗漏；回滚 Task 11 后端提交 |
| `framework.configure.mvc` | `MvcConfiguration` | `framework.web.configuration` | MVC 配置集中于 Web 配置子包 | MVC Bean 未装配；回滚 Task 11 后端提交 |
| `framework.configure.mvc` | `PasswordEncoderConfiguration` | `framework.security.configuration.authentication` | 密码编码属于安全认证配置 | PasswordEncoder Bean 未装配；回滚 Task 11 后端提交 |
| `framework.configure.mvc.advice.exception` | `CommonExceptionAdvice`, `KaptchaExceptionAdvice`, `SqlExceptionAdvice` | `framework.web.advice.exception` | MVC 异常处理按职责归位 | Advice 扫描遗漏；回滚 Task 11 后端提交 |
| `framework.configure.mvc.advice.request` | `RequestDecryptAdvice` | `framework.web.advice.crypto` | 请求解密按加解密职责归位 | 请求解密链路遗漏；回滚 Task 11 后端提交 |
| `framework.configure.mvc.advice.response` | `ResponseEncryptAdvice`, `ResponseModifyAdvice` | `framework.web.advice.crypto` | 响应处理按加解密职责归位 | 响应 Advice 扫描遗漏；回滚 Task 11 后端提交 |
| `framework.configure.mvc.crypto` | `CryptoKeyManager` | `framework.web.crypto` | Web 加解密密钥适配归位 | 密钥 Bean 装配遗漏；回滚 Task 11 后端提交 |
| `framework.configure.mvc.filter` | `RequestCorrelationFilter`, `RequestGetParamsFilter` | `framework.web.filter` | Web 过滤器归位 | Filter 装配遗漏；回滚 Task 11 后端提交 |
| `framework.configure.mvc.security` | `AuthenticationWebUtils` | `framework.web.security` | Cookie/CSRF HTTP 工具归位 | Core FQCN 引用遗漏；回滚 Task 11 后端提交 |
| `framework.configure.mybatis` | `MyBatisPlusConfiguration` | `framework.persistence.configuration` | 持久化配置集中于配置子包 | MyBatis Bean 未装配；回滚 Task 11 后端提交 |
| `framework.configure.mybatis` | `MetaObjectHandlerImpl` | `framework.persistence.mybatis` | MyBatis 审计填充实现归位 | 审计填充 Bean 未装配；回滚 Task 11 后端提交 |
| `framework.configure.mybatis` | `DataScopeContextFilter`, `DataScopeEntityRegistry`, `DataScopeExecutor` | `framework.persistence.scope.context` | 数据范围上下文职责归位 | 持久化组件扫描遗漏；回滚 Task 11 后端提交 |
| `framework.configure.mybatis.interceptor` | `DataScopeInnerInterceptor` | `framework.persistence.scope.authorization` | 数据权限 SQL 拦截归位 | MyBatis 拦截器链遗漏；回滚 Task 11 后端提交 |
| `framework.configure.mybatis.security` | `ResourceAuthorizationGuard`, `ScopeSqlPolicy` | `framework.persistence.scope.authorization` | 数据权限授权实现归位 | 授权策略引用遗漏；回滚 Task 11 后端提交 |
| `framework.configure.redis` | `RedisConfiguration` | `framework.cache.configuration` | 通用 Redis 属于缓存基础设施 | Redis Bean 未装配；回滚 Task 11 后端提交 |
| `framework.configure.security.advice` | `LoginExceptionAdvice`, `RestAccessDeniedHandler`, `RestAuthenticationEntryPoint` | `framework.security.advice` | 安全认证处理器归位 | 安全异常链路遗漏；回滚 Task 11 后端提交 |
| `framework.configure.security.autoconfiguration` | `SecurityAutoConfiguration` | `framework.security.configuration` | 安全能力组合入口归位 | AutoConfiguration FQCN 遗漏；回滚 Task 11 后端提交 |
| `framework.configure.security.configuration` | `SecJacksonConfiguration`, `SecRedisConfiguration` | `framework.security.configuration.redis` | 安全 Redis/Jackson 配置归位 | 安全基础 Bean 装配遗漏；回滚 Task 11 后端提交 |
| `framework.configure.security.configuration` | `SecurityConfiguration` | `framework.security.configuration.authentication` | Spring Security 认证配置归位 | 安全过滤链装配遗漏；回滚 Task 11 后端提交 |
| `framework.configure.security.configuration` | `SecuritySessionPortConfiguration` | `framework.security.configuration.session` | Session 端口配置归位 | Session 端口 Bean 装配遗漏；回滚 Task 11 后端提交 |
| `framework.configure.security.configuration` | `WebCookiePolicy` | `framework.web.security` | Cookie Web 工具归位 | Cookie 策略引用遗漏；回滚 Task 11 后端提交 |
| `framework.configure.security.converter` | `UserOnlineConverter` | `framework.security.converter` | 安全用户转换契约归位 | Security 转换器扫描遗漏；回滚 Task 11 后端提交 |
| `framework.configure.security.eval` | `SpectraPermissionEvaluator` | `framework.security.authorization` | 权限运行时实现归位 | 方法安全 Bean 未装配；回滚 Task 11 后端提交 |
| `framework.configure.security.filter` | `TokenAuthenticationFilter` | `framework.security.authentication` | Token 过滤器归位 | Token 认证链路遗漏；回滚 Task 11 后端提交 |
| `framework.configure.security.holder` | `SecurityLoginFailureTracker`, `SecuritySessionIssuer`, `SecuritySessionRevoker` | `framework.security.session.lifecycle` | Session 生命周期契约归位 | 安全端口 Bean 未装配；回滚 Task 11 后端提交 |
| `framework.configure.security.holder` | `SecuritySessionContextAccessor`, `SecuritySessionQuery`, `SecuritySessionReader` | `framework.security.session.query` | Session 查询和上下文契约归位 | 安全端口 Bean 未装配；回滚 Task 11 后端提交 |
| `framework.configure.security.holder` | `SecurityTokenAccessor` | `framework.security.session.token` | Token 访问契约归位 | Token 端口 Bean 未装配；回滚 Task 11 后端提交 |
| `framework.configure.security.properties` | `SecMode`, `SecurityProperties` | `framework.security.properties` | 安全模式和属性归位 | 配置绑定或默认值遗漏；回滚 Task 11 后端提交 |
| `framework.configure.security.ratelimit` | `RateLimitPolicy`, `RedisRateLimiter`, `RequestRateLimitFilter` | `framework.security.ratelimit` | 限流运行时归位 | fail-closed 限流链路遗漏；回滚 Task 11 后端提交 |
| `framework.configure.security.redis` | `RedisSecurityInitializationTokenStore`, `RedisSecurityVerificationStore`, `RefreshTokenRotationStore` | `framework.security.redis.store` | Redis 安全存储适配归位 | 安全 Redis 组件装配遗漏；回滚 Task 11 后端提交 |
| `framework.configure.security.redis` | `SecurityRedisExecutor`, `SecurityRedisKey`, `SecurityRedisNamespace` | `framework.security.redis.key` | Redis Key 与执行器归位 | 安全 Redis Key 引用遗漏；回滚 Task 11 后端提交 |
| `framework.configure.security.redis` | `TokenDigestService` | `framework.security.redis.token` | Token 摘要工具归位 | Token 摘要引用遗漏；回滚 Task 11 后端提交 |
| `framework.configure.security.root` | `DefaultRootAuthorizationPolicy` | `framework.security.authorization` | 权限运行时实现归位 | Root 策略 Bean 未装配；回滚 Task 11 后端提交 |
| `framework.configure.security.strategy` | `RedisSecuritySessionRepository` | `framework.security.session.repository` | Session Redis 实现归位 | Session 端口实现遗漏；回滚 Task 11 后端提交 |

## 自动配置与运行配置

- `spectra-launch` 的启动入口和 Flyway 装配属于组合根。
- Security 的 Spring Boot 自动配置属于 `spectra-framework`，由唯一的 `FrameworkModule` 通过 `@ComponentScan(basePackageClasses = FrameworkModule.class)` 扫描 `framework` 根包；各能力配置使用自身的 `@Configuration`、`@Bean` 和 `@EnableConfigurationProperties` 声明，不再设置能力级 `AssemblerAutoConfiguration`。该扫描只覆盖 framework 技术 Bean，不会把业务实现隐式注入 core。统一 Log 技术切面和业务编排属于 `spectra-core`。
- Core 通过自己的 `AutoConfiguration.imports` 注册唯一 Core 入口，入口扫描 `com.devops00.spectra.core` 下全部核心子域；Workflow、OA 由 `spectra.modules.<name>.enabled` 控制，缺省保持启用。`spectra.notification.enabled` 只控制通知业务功能，不控制 Core 装配。`spectra-launch` 在上下文创建阶段校验 OA/Workflow 的必需 adapter，Core 始终装配。
- `spectra-config` 集中 `application-*.yml` 和 Flyway 资源；02-Task 1 已对源码、配置、POM 和运行时依赖树复核，未发现 Nacos、Spring Cloud、Eureka、Consul、Zookeeper 或远程配置中心生效项；02-Task 2/3 和本次 Task 4 已将清理结论固化为持续门禁，不需要删除当前不存在的组件。
- Redis 的 Token、Session、验证码、防重放和登录失败锁定仍由安全模块维护，并继续遵守安全 Redis fail-closed 约束。
- 本任务不处理租户配置；应用层不允许新增租户注册、租户切换、租户管理员、租户数据源、租户级缓存 key 或租户 API 参数。

### 02-Task 1 扫描记录

| 扫描范围 | 命令/证据 | 结果 | 处理结论 |
|---|---|---|---|
| 源码、配置、POM 关键词 | `rg -n -i "nacos\|discovery\|service-registry\|spring-cloud\|config-server\|eureka\|consul\|zookeeper" spectra-admin`，排除 `target` 和 `.mise.local.toml` | 0 命中 | 无生效代码、配置或文档遗留需要删除；保留关键词门禁 |
| Maven 运行时依赖树 | `cd spectra-admin && ./mvnw dependency:tree -Dverbose`，使用 mise 加载 Java 25 和可写本地仓库 | 退出码 0，禁止客户端 0 命中 | 运行时未引入 Nacos、Discovery、Config Client、Eureka、Consul 或 Zookeeper 客户端 |
| 自动配置/启动入口 | `spectra-launch`、`spectra-config`、starter 自动配置入口和 `pom.xml` | 0 个微服务客户端入口 | launch 只做组合和校验；架构测试与边界脚本持续阻断回归 |

本次扫描没有“合法业务词汇误命中”或“无效遗留配置命中”；因此不存在需要逐条迁移/删除的微服务组件。后续新增模块仍必须通过同一关键词扫描和 Maven 依赖门禁，不允许以远程配置、注册中心或服务发现替代 launch 的本地模块装配。架构门禁当前结果为 `ArchitectureRulesChecked=7`、`ArchitectureViolations=0`。

## 后续迁移批次索引

| 批次 | 处理方式 | 对象 | 目标 | 后续任务 |
|---|---|---|---|---|
| B00 | 无需移动 | 已经位于正确边界的 core 核心领域、各可选模块领域闭环、framework 技术机制、launch 组合根和 config 资源 | 保持当前位置，后续只补门禁和端口 | 03、06 |
| B01 | 直接移动 | common 中确认属于 MyBatis、MVC、数据库或 Spring Event 的具体桥接类 | 已完成：`BaseEntity`/`BaseService*`/`PageFrom`/JSONB Handler/`UUIDTypeHandler` 归 framework.persistence，`R`/`SystemProperties` 归 framework，系统配置枚举和写入端口归 core.system，上传完成事件改为 common 纯数据 | 03、06；Task 25 |
| B02 | 抽取公共端口 | core → notification 依赖、OA 对 core 内部 Entity/Mapper/Service 的引用 | 由 common 或 core 对外稳定端口承载契约，core 不依赖可选业务实现 | 03、06 |
| B03 | 抽取公共端口 | 原独立 Security 模块的技术端口、framework 安全自动配置、core 安全策略/审计编排交界 | common 保留安全公共端口，framework 保留 Token/Session/过滤器和安全 Redis 机制，core 保留安全策略、用户权限和安全审计编排 | 03、04 |
| B04 | 抽取公共端口 | 三份模块专属 HealthIndicator 及健康聚合 | 统一 contributor 协议和 core 聚合入口，实现留在 notification/upload/core | 05 |
| B05 | 删除重复实现 | 旧日志模块、旧 ULog 类型和普通操作日志重复链路 | 已完成：`@Audit`/`AuditService` 是唯一业务入口；技术切面、`SysLogType`、普通日志 outbox 和 worker 统一归 `spectra-core`；安全审计链路保留同步失败阻断 | 04、06 |
| B06 | 抽取公共端口 | OA/Upload/Workflow 的跨模块内部包引用 | 以 Facade、Port 或事件替换具体 Entity、Mapper、实现类引用，保持可选模块可拆装 | 03、06 |
| B07 | 延期评估 | 无法从当前调用证据判断归属的 `core/common` 零散辅助类和未来 ERP | 完成使用图和启动验证后再决定；ERP 延后到 1.0.0 稳定后 | 06、1.0.0 后 |

每个批次的前置依赖、影响模块、测试范围和回滚点见 [[02-能力归属矩阵#迁移批次执行登记]]。本清单冻结后，任何搬迁必须先在矩阵登记，不允许临时凭经验移动。
