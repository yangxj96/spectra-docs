---
tags:
  - backend
  - crypto
  - security
created: 2026-07-11
---

# 接口加解密方案

> 当前实现：RSA-OAEP + AES-256-GCM 请求/响应加密、Redis nonce 防重放、运行时密钥管理和 Web 客户端接入。
>
> 基于 RSA + AES-GCM 混合加密的前后端接口数据加解密方案。

## 加密协议

```
请求加密：AES-256-GCM 随机密钥加密请求体 → RSA-OAEP 公钥加密 AES 密钥
响应加密：AES-256-GCM 随机密钥加密响应体 → RSA-OAEP 公钥加密 AES 密钥 → RSA 私钥签名
```

## 加密数据格式

```json
{
  "data": "AES-GCM 加密后的 Base64 密文",
  "key": "RSA-OAEP 加密后的 AES 密钥 (Base64)",
  "iv": "AES-GCM 初始化向量 (Hex)",
  "nonce": "随机数 (Base64)",
  "timestamp": 1234567890,
  "signature": "RSA 签名 (Base64)"
}
```

## 响应处理流程

```
Controller 返回业务数据
  → ResponseEncryptAdvice (@Order HIGHEST_PRECEDENCE)
    → AES-GCM 加密数据 → RSA 加密 AES 密钥 → RSA 签名
    → 返回 {data, key, iv, nonce, timestamp, signature} Map
  → ResponseModifyAdvice (@Order HIGHEST_PRECEDENCE + 1)
    → 包装为 R.success(加密Map)
    → 最终: {code:200, msg:"success", data: {data:"...", key:"...", ...}}
```

## 请求处理流程

```
HTTP 请求到达
  → MessageConverter 准备反序列化
  → RequestDecryptAdvice.beforeBodyRead()
    → 检查是否为加密请求（含 data + key + iv 字段）
    → RSA 验签 → RSA 私钥解密 AES 密钥 → AES-GCM 解密 data
    → 返回解密后的 body 供后续反序列化
  → Controller 收到正常的 DTO
```

## 目录结构

```
spectra-admin/spectra-common/src/main/java/com/devops00/spectra/common/
├── annotation/
│   └── Encrypt.java                    ← 加解密标记注解（value + response 控制）
├── utils/
│   ├── AESUtils.java                   ← AES-256-GCM 加解密
│   ├── RSAUtils.java                   ← RSA-2048-OAEP 加解密 + 签名/验签
│   └── SHA256Utils.java                ← SHA256 摘要 / HMAC / Nonce 生成
└── exception/
    └── EncryptException.java           ← 加密异常

spectra-admin/spectra-framework/src/main/java/com/devops00/spectra/framework/configure/mvc/
├── crypto/
│   └── CryptoKeyManager.java           ← 密钥管理（JdbcTemplate 读 sys_config + 内存缓存）
├── MvcConfiguration.java               ← 注册 Bean
└── advice/
    ├── request/
    │   └── RequestDecryptAdvice.java   ← 请求体解密 Advice
    └── response/
        ├── ResponseEncryptAdvice.java  ← 响应体加密 Advice
        └── ResponseModifyAdvice.java   ← 统一 R 包装 Advice

spectra-ui/src/
├── api/system/
│   └── crypto.ts                       ← 密钥管理 API 与平台加解密实现
├── plugin/store/modules/
│   └── use-crypto-store.ts             ← Pinia store（密钥状态 + 持久化）
├── utils/crypto/
│   ├── crypto-utils.ts                 ← 一键加密/解密/签名/验签
│   ├── aes-utils.ts                    ← AES-GCM 前端实现
│   └── ras-utils.ts                    ← RSA 前端实现
└── plugin/request/
    └── http.ts                         ← HTTP 客户端（集成 encryptRequest/decryptResponse）
```

## 配置

密钥存储在 `sys_config` 数据库表，通过后端 API 管理，前端按需获取：

```
sys_config 表：
├── crypto.enabled              = true/false（加解密总开关）
├── crypto.server.public-key   = 服务端公钥（前端加密请求用）
├── crypto.server.private-key  = 服务端私钥（后端解密请求用）
├── crypto.client.public-key   = 客户端公钥（后端加密响应用）
└── crypto.client-private-key  = 客户端私钥（前端解密响应用，通过 API 获取）
```

Web 前端不再配置加解密环境变量：启动阶段由 `GET /api/system/bootstrap` 获取当前开关，DEV_OPS 首次登录后的系统设置引导负责保存开关和密钥。

## 实现组成

### 核心加密链路

- `CryptoKeyManager`：通过 JdbcTemplate 读取 `sys_config`，使用内存缓存和原子替换管理密钥。
- `ResponseEncryptAdvice`：注入 `CryptoKeyManager`，按运行时 `isEnabled()` 判断是否加密响应。
- `RequestDecryptAdvice`：注入 `CryptoKeyManager`，按运行时 `isEnabled()` 解密请求。
- `SMProperties` 已移除，旧 yml 密钥配置已清理。

### 客户端与异常处理

- `@Encrypt` 提供细粒度加密控制。
- 前端 `encryptRequest` 生成 RSA 签名，`verifySignature` 与后端算法参数保持一致。
- 加密失败抛出 `EncryptException`；空请求体直接返回 `null`；`encryptRequest` 接受 `unknown` 并在内部序列化。

### 防重放与密钥管理

- `RequestDecryptAdvice` 使用 ±5 分钟时间窗口和 Redis nonce 去重，拒绝重放请求。
- 密钥管理 API 提供 `POST /system/crypto/keypair/generate`、`GET /system/crypto/keypair/public` 和客户端私钥获取接口。
- `RequestDecryptAdvice` 优先检查 `X-Encrypted` 请求头，再判断请求结构。
- 现有单元测试覆盖后端 11 个用例和前端 8 个用例；`CryptoKeyManager` 与 `CryptoController` 新端点尚无专门单元测试。

## 关键文件

| 文件 | 路径 | 说明 |
|---|---|---|
| AESUtils | `spectra-common/.../utils/AESUtils.java` | AES-256-GCM 加解密 |
| RSAUtils | `spectra-common/.../utils/RSAUtils.java` | RSA 加密 + 签名验签 |
| SHA256Utils | `spectra-common/.../utils/SHA256Utils.java` | 摘要 / HMAC / Nonce |
| EncryptException | `spectra-common/.../exception/EncryptException.java` | 加密异常 |
| Encrypt | `spectra-common/.../annotation/Encrypt.java` | 加解密标记（value + response） |
| CryptoKeyManager | `spectra-framework/.../crypto/CryptoKeyManager.java` | 密钥管理（sys_config + 内存缓存） |
| ResponseEncryptAdvice | `spectra-framework/.../advice/response/ResponseEncryptAdvice.java` | 响应加密 |
| RequestDecryptAdvice | `spectra-framework/.../advice/request/RequestDecryptAdvice.java` | 请求解密 |
| CryptoController | `spectra-core/.../controller/CryptoController.java` | 密钥管理 API（config/generate/refresh） |
| use-crypto-store | `spectra-ui/src/plugin/store/modules/use-crypto-store.ts` | 密钥 Pinia store |
| crypto.ts | `spectra-ui/src/api/system/crypto.ts` | 密钥管理 API + getter |
| http.ts | `spectra-ui/src/plugin/request/http.ts` | HTTP 客户端（集成加解密） |

## 运行时密钥管理：`sys_config`

> 当前实现：RSA 密钥存储在 `sys_config`，支持运行时加载、缓存和热更新。
>
> 目标：将 RSA 密钥从 `application.yml` 硬编码迁移至 `sys_config` 表动态管理，支持运行时热更新。

### 背景

方案一存在的安全问题：
- 前端 `.env` 中硬编码 RSA 私钥，通过 `VITE_` 前缀编译进 JS bundle，DevTools 可直接获取
- 密钥轮换需改 yml + 重启服务 + 重新构建前端
- 前后端共享同一对密钥，签名无身份证明价值

方案二改进：
- 后端密钥存入 `sys_config` 表，前端通过 API 按需获取
- 前端不持有持久化私钥，仅在内存中缓存，刷新即失效
- 密钥轮换通过系统配置管理页面即时生效，无需重启

### sys_config 密钥命名

| key | value 示例 | 用途 | API 暴露 |
|---|---|---|---|
| `crypto.enabled` | `true` | 加解密总开关 | `GET /system/crypto/config` |
| `crypto.server.public-key` | `MIIBIjAN...` | 服务端公钥（前端加密请求） | `GET /system/crypto/config` |
| `crypto.server.private-key` | `MIIEvQIB...` | 服务端私钥（后端解密请求） | 不暴露 |
| `crypto.client.public-key` | `MIIBIjAN...` | 客户端公钥（后端加密响应） | 不暴露 |
| `crypto.client.private-key` | `MIIEvQIB...` | 客户端私钥（前端解密响应） | `GET /system/crypto/keypair/client-private` |

### 架构对比

| 方面 | 方案一（yml 硬编码） | 方案二（sys_config 动态） |
|---|---|---|
| 私钥存储 | 前端 JS bundle 明文 | 仅浏览器内存 |
| 密钥轮换 | 改 yml + 重启 + 重构建 | 系统配置页面修改 → 即时生效 |
| 加密开关 | yml 中 `spectra.system.sm.enabled` | `sys_config.crypto.enabled` |
| 服务端私钥 | yml 明文 | sys_config（数据库级别权限隔离） |
| 性能 | 启动时一次性解析 | 启动加载到内存，请求时零开销 |

### CryptoKeyManager 设计

**位置**：`spectra-framework/configure/mvc/crypto/CryptoKeyManager.java`

**核心设计**：用 `JdbcTemplate` 直读 `sys_config` 表（避免依赖 `spectra-core` 导致循环引用），密钥对象用不可变 wrapper + `volatile` 原子替换保证线程安全。

```
CryptoKeyManager
├── @PostConstruct init() → 从 sys_config 加载密钥到内存
├── isEnabled() → 检查 crypto.enabled + 密钥完整性
├── getServerPublicKey() → 返回内存缓存的 PublicKey
├── getServerPrivateKey() → 返回内存缓存的 PrivateKey
├── getClientPublicKey() → 返回内存缓存的 PublicKey
├── getClientPrivateKey() → 返回内存缓存的 PrivateKey
├── getServerPublicKeyBase64() → 返回 Base64 字符串
├── getClientPrivateKeyBase64() → 返回 Base64 字符串
└── refresh() → 重新从 sys_config 加载
```

**线程安全方案**：

```java
private record CryptoKeys(
    PublicKey serverPub, PrivateKey serverPri,
    PublicKey clientPub, PrivateKey clientPri
) {}
private volatile CryptoKeys keys;  // 原子替换，无部分更新窗口
```

**降级策略**：
- sys_config 无数据 → keys 为 null → `isEnabled()` 返回 false → 加解密透传
- DB 连接失败 → 静默失败，不阻塞启动

### 密钥数据流

```
启动时：
  sys_config (DB) → CryptoKeyManager.init() → 内存缓存

请求时：
  ResponseEncryptAdvice → cryptoKeyManager.getClientPublicKey() → 内存（微秒级）
  RequestDecryptAdvice → cryptoKeyManager.getServerPrivateKey() → 内存（微秒级）

密钥轮换：
  管理员修改 sys_config → CryptoController → cryptoKeyManager.refresh()
    → 清空旧内存 → 从 sys_config 重载 → 原子替换
```

### CryptoController API 变更

| 端点 | 方法 | 认证 | 说明 |
|---|---|---|---|
| `GET /system/crypto/config` | 新增 | 无 | 返回 `{enabled, serverPublicKey}`，前端初始化调用 |
| `GET /system/crypto/keypair/server-public` | 保留 | 无 | 返回服务端公钥 |
| `GET /system/crypto/keypair/client-private` | 新增 | 需登录 | 返回客户端私钥；临时密码会话也允许调用，用于完成首次密码修改时的加密通信 |
| `POST /system/crypto/keypair/generate` | 修改 | `ROLE_DEV_OPS` | 生成两对密钥 → 写入 sys_config → refresh |
| `POST /system/crypto/keypair/refresh` | 新增 | `ROLE_DEV_OPS` | 手动重新加载密钥到内存 |

### 前端初始化时序

```
应用启动
  → GET /system/crypto/config → { enabled: true, serverPublicKey: "..." }
    → enabled=false → 全局跳过加解密
    → enabled=true → 缓存 serverPublicKey → 后续请求加密可用

用户登录
  → POST /security/authentication/login  (@Encrypt(false)，明文响应)
  → GET /system/crypto/keypair/client-private (带 Token)
    → 缓存 clientPrivateKey → 后续响应解密可用
```

临时密码登录时，Token 会被限制在密码修改流程内，但仍允许获取客户端私钥、读取个人信息和加载必要的页面上下文；完成密码修改后旧会话会被撤销。客户端私钥接口只要求已登录，不要求 `security:crypto:manage`，因为它是所有启用接口加密的 Web 客户端都必须完成的通信初始化步骤，不属于密钥生成或刷新管理操作。

### 实现组成（10 个文件）

| # | 文件 | 操作 | 说明 |
|---|---|---|---|
| 1 | `SMProperties.java` | 删除 | 不再需要 yml 配置 |
| 2 | `CryptoKeyManager.java` | 新建 | `spectra-framework/configure/mvc/crypto/`，JdbcTemplate 读表 + 内存缓存 |
| 3 | `ResponseEncryptAdvice.java` | 修改 | 移除 `@ConditionalOnProperty`；注入 `CryptoKeyManager`；第 166 行改用 `clientPublicKey` |
| 4 | `RequestDecryptAdvice.java` | 修改 | 同上 |
| 5 | `CryptoController.java` | 修改 | 新增 `config`/`client-private`/`refresh` 端点；`generateKeyPair` 写 sys_config + refresh |
| 6 | `core/security/authentication/controller/AuthenticationController.java` | 修改 | `@Encrypt(false)` on `login()` |
| 7 | `spectra-ui/.env` | 修改 | 移除 `VITE_RSA_PUBLIC_KEY` / `VITE_RSA_PRIVATE_KEY` |
| 8 | `spectra-ui/http.ts` | 重构 | 启动时调 `GET /system/crypto/config`；登录后调 `GET /system/crypto/keypair/client-private` |
| 9 | `spectra-ui/crypto-utils.ts` | 重构 | 签名/解密入参动态化 |
| 10 | `spectra-ui/env.d.ts` | 修改 | 移除密钥类型声明 |

### 风险分析

| 风险等级 | 问题 | 处理方式 |
|---|---|---|
| 高 | 循环依赖（CryptoKeyManager 需读 sys_config） | `JdbcTemplate` 直读，不依赖 spectra-core |
| 高 | 启动时 sys_config 无数据 | 静默降级，`isEnabled()` 返回 false |
| 中 | 线程安全（refresh 期间部分更新） | 不可变 wrapper + volatile 原子替换 |
| 中 | `enabled=true` 但密钥缺失 | `isEnabled()` 附加密钥完整性检查 |
| 低 | 密钥变更竞态（请求加密用旧 key，后端解密用新 key） | v1 接受，不做处理 |
| 低 | 前端登录前 POST 响应加密 | 登录前端点已豁免，不受影响 |

### 实现核对清单

#### 核心运行时改动

- 新建 `CryptoKeyManager`（spectra-framework，JdbcTemplate 读 `sys_config` + 内存缓存 + 原子替换）。
- 修改 `ResponseEncryptAdvice`（移除 `@ConditionalOnProperty`；注入 `CryptoKeyManager`；`supports()` 检查 `isEnabled`）。
- 修改 `RequestDecryptAdvice`，与响应链路使用同一运行时密钥状态。
- 删除 `SMProperties` 并从 `MvcConfiguration` 移除引用。

#### API 与客户端改动

- 修改 `CryptoController`（提供 config/client-private/refresh 端点；`generateKeyPair` 写入 `sys_config`）。
- `AuthenticationController.login()` 使用 `@Encrypt(false)`。
- 前端 `http.ts` 在启动时获取 config，登录后获取 clientPrivateKey。
- 前端 `crypto-utils.ts` 使用动态密钥参数。

#### 配置与验证

- 前端 `.env` 已清理，并同步更新 `env.d.ts` 类型声明。
- `CryptoKeyManager` 与 `CryptoController` 新端点的专门单元测试仍未覆盖，当前依赖现有集成测试和运行时验收。

### 相关笔记

- [[80-基础设施]] — Redis / Jackson 等基础设施
- [[10-架构分层]] — Maven 多模块架构
- [[20-用户与权限]] — 安全框架
