---
tags:
  - guide
  - devops
---

# 环境搭建

> Spectra 项目完整开发环境搭建指南。

## 前置依赖

| 依赖 | 版本 | 安装方式 |
|---|---|---|
| JDK | 25 (Temurin) | 通过 mise 或手动安装 |
| Node.js | 24.14.0 | 通过 mise 管理 |
| pnpm | 11.0.9 | 通过 mise 管理 |
| Maven | 3.9.12 | 项目自带 `mvnw` wrapper |
| PostgreSQL | 18 | Docker 或本地安装 |
| Redis | — | Docker 或本地安装 |

## mise 配置

项目使用 [mise](https://mise.jdx.dev/) 管理运行时版本。

根目录 `mise.toml` 定义全局工具版本。`spectra-admin/.mise.local.toml` 定义后端专用配置：

```toml
# spectra-admin/.mise.local.toml （从 .example 复制）
SERVER_PORT=4004
DB_URL="jdbc:postgresql://127.0.0.1:5432/devops00_spectra_db"
DB_USERNAME="your_username"
DB_PASSWORD="your_password"
REDIS_HOST="127.0.0.1"
REDIS_PORT=6379
# S3 配置...
# AI 配置...
```

## 初始化步骤

### 1. 配置后端

```bash
# 进入 spectra-admin 目录
cd spectra-admin

# 复制环境配置模板
copy .mise.local.toml.example .mise.local.toml

# 编辑 .mise.local.toml，填入实际的数据库/Redis/S3/AI 凭据
```

### 2. 配置前端

```bash
# spectra-ui（默认已配置，指向 localhost:4004）
cd spectra-ui
# .env 已存在，如需修改：
# VITE_API_URL=https://127.0.0.1:4004/

# spectra-app（默认已配置）
cd spectra-app
# .env.development 已存在：
# VITE_API_BASE_URL=https://127.0.0.1:4004
```

### 3. 启动数据库和 Redis

确保 PostgreSQL 和 Redis 服务已启动。

### 4. 启动后端 API

```bash
cd spectra-admin
./mvnw spring-boot:run -pl spectra-launch
# API 运行在 https://127.0.0.1:4004
```

### 5. 启动前端

```bash
# Web 管理后台
cd spectra-ui
pnpm install
pnpm start           # → http://localhost:5173

# 移动端 H5
cd spectra-app
pnpm install
pnpm start           # → http://localhost:xxxx
```

## 本地只读数据库

开发时可使用只读数据库账号查询：

| 配置项 | 值 |
|---|---|
| 数据库 | PostgreSQL 18 |
| 地址 | `127.0.0.1:5432` |
| 数据库名 | `devops00_spectra_db` |
| 用户名 | `ai` |
| 密码 | `QuVsKppcWvwwX2Vv` |

## 相关笔记

- [[00-项目总览]]
- [[20-常见命令]]
- [[80-基础设施]]
