# Spectra AI 开发环境与项目完整检测报告

- 检测周期：2026-08-09 至 2026-08-10
- 检测范围：根仓库、`spectra-admin`、`spectra-ui`、`spectra-app`、`logicflow-plugin-flowable`、项目 Codex 插件、固定运行时、CodeGraph、文档、依赖安全、构建、测试与运行态
- 检测方式：固定版本工具链、CodeGraph/源码静态检查、完整构建测试、npm 与 OWASP 供应链检查、真实 PostgreSQL/Redis 启动验证、Web/App 浏览器冒烟
- 安全边界：未读取、输出或提交 `.mise.local.toml`、密码、Token、私钥及本机凭据；未执行 Git push

## 一、最终结论

当前 AI 开发环境正常，项目框架和四个工程的职责、依赖关系、运行端口、构建产物均符合项目文档。已要求修复的 P1、P2、P3 问题均已处理并完成回归；后端连接真实 PostgreSQL/Redis 的完整测试和运行态健康检查通过，Web、App 与流程插件的全量静态检查和生产构建通过。

| 领域 | 最终状态 | 结论 |
|---|---|---|
| 固定运行时 | 通过 | PowerShell 7.6.4、Java 25.0.2、Node 24.14.0、pnpm 11.0.9、Maven Wrapper 3.9.12 符合项目约束 |
| PowerShell 策略 | 通过 | 项目 PowerShell 脚本强制 7.6+，文档明确不支持 Windows PowerShell 5.1 |
| 后端 | 通过 | Spotless、17 模块全量测试、跳过测试打包、HTTPS 启动、PostgreSQL/Redis 健康检查通过 |
| Web | 通过 | 格式、lint、类型、31 个测试、生产构建通过；生产依赖审计为 0 |
| App | 通过，风险已接受 | 格式、lint、类型、H5 和微信小程序构建在启动前后均通过；DCloud 固定依赖链 24 条公告按用户决定暂不升级 |
| 流程插件 | 通过 | 格式、ESM/CJS/CSS/DTS 构建通过，显式 `any` 为 0，生产依赖审计为 0 |
| 运行态 | 通过 | 后端 health 200 且 DB/Redis/liveness/readiness 均 UP；Web 和 App 登录页浏览器冒烟无运行错误 |
| CodeGraph | 通过 | 项目保留 CodeGraph 作为源码定义、调用链和影响范围查询工具 |
| Graphify | 已移除 | Hook、项目 skill、生成目录和常规文档入口均已移除 |
| 文档 | 通过 | 项目知识库检查通过，Entity/Controller 数量与源码一致 |
| Git | 通过 | 四个子项目已分别提交，根仓库在本报告完成后提交，不 push |

项目可继续正常开发。剩余事项均有明确边界：DCloud/uni-app 依赖整套升级、短信/邮件发送占位、EmbedPDF 单一动态引擎块体积、首次 NVD 数据库同步以及需要真实账号/外部 Provider 的业务验收。

## 二、用户确认的排除项

以下内容不是本轮缺陷，按用户最新决定保留：

1. App 的 DCloud/uni-app 依赖版本较旧，不能单独升级 Vite、vue-i18n 或其中一部分；现有 24 条生产依赖公告作为已接受风险记录。
2. 后端短信/邮件发送和 Web 账号绑定验证码共 4 处 TODO 目前仅为占位，不做实现。
3. 需要 NVD API Key 才能高效完成的后端首次漏洞库同步，在无 Key 环境中停止，保留本地命令和 CI 门禁供人工复跑。

## 三、P1/P2/P3 修复结果

### 3.1 P1：安全与数据泄露

| 问题 | 修复 | 验证 |
|---|---|---|
| 表单设计器传递依赖 `wangeditor@4.7.15` 存在全版本 XSS 公告 | 富文本入口停用；导入/保存规则拒绝 `fcEditor`、`fc-editor`、`wangeditor`；Vite 使用安全占位组件；pnpm 将上游组件依赖替换为仓库内无 HTML 渲染能力的禁用包 | `wangeditor` 已从生产依赖树移除，Web audit 从 1 high / 1 moderate 降为 0 |
| LogicFlow 传递依赖 `uuid@9.0.0` 存在缓冲区边界公告 | pnpm workspace override 固定为已修复的 `uuid@11.1.1` | 完整 Web 测试和构建通过，audit 为 0 |
| 审计日志会把 `refresh_token` 原文写入 `sys_log.args` 和 MyBatis DEBUG 参数 | 新增 `AuditLogSanitizer`，在持久化和长度截断前递归脱敏 Map、集合、数组、对象、Bearer Token 和 URL 查询凭据 | 3 个新增单元测试通过；运行态 canary 中只出现 `***`，未出现原始值 |
| Redis 配置日志可能输出完整连接 URL/密码 | 配置日志不再输出连接 URL 或其他凭据 | 启动日志不再出现 Redis 凭据 |
| S3 测试日志会输出对象名和预签名 URL | 移除敏感 URL/对象名日志，保留数量断言 | 后端全量测试通过 |

### 3.2 P2：构建、质量与供应链

| 问题 | 修复 | 验证 |
|---|---|---|
| WorkflowDesigner 约 1.96 MB，流程相关依赖耦合在同一大块 | 按 LogicFlow core、extension、本地 Flowable 插件、表单设计器和路由组件拆分 | Flowable 插件块约 1.21 MB、WorkflowDesigner 路由块约 14 KB，均低于 1.5 MB 警戒线 |
| 流程插件约 70 处显式 `any` 类型逃逸 | 建立 LogicFlow/BPMN 局部类型边界并清理显式 `any` | 源码扫描为 0，ESM/CJS/DTS 全部构建通过 |
| 后端缺少依赖 CVE 门禁 | 新增 OWASP Dependency-Check 12.2.2 的 `security-audit` Maven profile、CVSS 7 门禁、HTML/JSON/SARIF 报告和 GitHub Actions 工作流 | 插件可启动并开始同步 NVD；CI 支持 NVD Key 和数据库缓存 |
| 会修改本地业务数据的初始化测试混入普通全量测试 | Region/System/User 等手工初始化类及上传类型初始化方法改为显式 disabled，保留人工执行入口 | 全量测试不再隐式写入种子数据，共 17 个手工测试跳过 |
| 工作流表单 JSON 存在重复编码风险 | 修正表单 JSON 导入、校验和保存边界 | 新增安全测试，Web 31 个测试通过 |

### 3.3 P3：开发体验与可维护性

| 问题 | 修复 | 验证 |
|---|---|---|
| Windows PowerShell 5.1 可能被误用 | 根脚本增加 `#requires -Version 7.6`，文档和命令固定使用 `C:\Program Files\PowerShell\7\pwsh.exe` | PowerShell 7.6.4 下脚本与全量命令通过 |
| Graphify 在当前环境不可用且与 CodeGraph 职责重复 | 删除 Graphify hook、skill、生成目录和日常入口；源码级查询统一使用 CodeGraph | 根配置不再依赖 Graphify |
| `GuoMiUtilTest` 实现不标准 | 按用户决定直接删除 | 后端全量测试和打包通过 |
| App 的远程配置、更新检查、错误上报和 Android 权限仍为占位 | 补齐类型化实现边界、默认行为、错误处理和 Android appid/权限守卫 | App 源码 TODO 降为 0，lint 0 warning，双平台构建通过 |
| Web/App/插件存在类型逃逸和测试误连后端 | 清理显式 `any`，修正 mock 路径和默认导出假设 | 三个 TypeScript 工程显式 `any` / `@ts-ignore` 扫描均为 0 |
| UI/App Husky 脚本使用 v9 已弃用引导 | 移除会在 Husky v10 失败的旧引导行，保留 lint-staged 与 commitlint 命令 | amend 提交时钩子正常执行且不再打印弃用告警 |

## 四、框架与架构符合性

- 根仓库仍由一个 Spring Boot API、两个 Vue 客户端和一个 LogicFlow 插件组成，目录关系与项目总览一致。
- `spectra-admin` 为 17 模块 Maven Reactor，分层为 config、common、starter、framework、modules、launch；Spring Boot 4.1 / Java 25 组合可编译、测试、打包和启动。
- `spectra-ui` 使用 Vue 3.5.28、Vite 8.0.11、Element Plus 2.13.2，并通过 `file:../logicflow-plugin-flowable` 使用本地插件。
- `spectra-app` 使用当前 DCloud 固定的 uni-app、Vue 3.4.21、Vite 5.2.8，H5 与微信小程序构建链一致。
- `logicflow-plugin-flowable` 同时输出 ESM、CJS、CSS、source map 和 TypeScript 声明。
- Web 与 App 均连接后端 `https://127.0.0.1:4004/api`，开发端口 5173 / 5174 与文档一致。
- 数据库 Entity 和 Controller 数量未因本轮修复发生变化，文档统计仍为 60 / 40。

未发现模块边界、端口约定、子项目连接关系或文档统计上的结构性偏差。

## 五、完整验证矩阵

### 5.1 后端 `spectra-admin`

| 验证项 | 结果 |
|---|---|
| `.\mvnw.cmd spotless:check` | 通过，17 模块 SUCCESS |
| `.\mvnw.cmd test` | 通过，17 模块 SUCCESS |
| Surefire 汇总 | 66 个测试：49 执行通过、17 个手工初始化测试跳过、0 failure、0 error |
| `spectra-framework` | 16/16 通过 |
| `spectra-log-base` | 3/3 通过 |
| `spectra-core` + `spectra-upload` | 47 个发现，30 个执行通过，17 个手工初始化测试跳过 |
| `.\mvnw.cmd package -DskipTests` | 通过，17 模块 SUCCESS，生成 `spectra-launch-0.0.18.jar` |
| HTTPS 启动 | Java 25.0.2、dev profile、Tomcat 4004、context path `/api` 启动成功 |
| Actuator health | HTTP 200；overall、db、redis、ssl、liveness、readiness、diskSpace、ping 均 UP |
| 审计脱敏 canary | 预期拒绝无效刷新令牌；数据库插入参数包含 `***` 且不含原始 canary |

测试使用用户已启动的真实 PostgreSQL 和 Redis。普通全量测试只跳过明确标记的手工种子/初始化用例，避免自动改变开发数据库业务数据。

### 5.2 Web `spectra-ui`

| 验证项 | 结果 |
|---|---|
| `pnpm run format:check` | 通过 |
| `pnpm run lint` | 通过 |
| `pnpm run type-check` | 通过 |
| `pnpm run test` | 9 files、31 tests 全部通过 |
| `pnpm run build` | 通过 |
| `pnpm audit --prod --registry=https://registry.npmjs.org/` | 0 vulnerability，324 个生产依赖、3 个 optional dependency |
| 浏览器冒烟 | 登录页标题、账号输入和登录按钮正常，无控制台运行错误 |

关键构建块：

- `WorkflowDesigner` 路由约 13.99 KB。
- `logicflow-core` 约 354.12 KB。
- `logicflow-extension` 约 474.74 KB。
- `flowable-plugin` 约 1,210.56 KB。
- `form-designer` 约 1,271.73 KB。
- `pdf-snippet` 约 2,082.76 KB，为当前唯一超过 1.5 MB 的块。

### 5.3 App `spectra-app`

| 验证项 | 启动前 | 服务运行期间复验 |
|---|---|---|
| `pnpm run format:check` | 通过 | 通过 |
| `pnpm run lint` | 通过，0 warning | 通过，0 warning |
| `pnpm run type-check` | 通过 | 通过 |
| `pnpm run build:h5` | 通过 | 通过 |
| `pnpm run build:mp-weixin` | 通过 | 通过 |
| H5 浏览器冒烟 | — | 欢迎页、登录按钮、验证码 data image 正常，无运行错误 |
| `pnpm audit --prod` | 6 high、15 moderate、3 low、0 critical | 用户接受的 DCloud 固定依赖风险 |

### 5.4 流程插件 `logicflow-plugin-flowable`

| 验证项 | 结果 |
|---|---|
| `pnpm run format:check` | 通过 |
| `pnpm run build` | 通过，ESM/CJS/CSS/DTS 均生成 |
| 显式 `any` / TypeScript 抑制 | 0 |
| `pnpm audit --prod` | 0 vulnerability，44 个生产依赖 |

### 5.5 文档、脚本与代码智能

| 验证项 | 结果 |
|---|---|
| `.\scripts\check-docs.ps1` | 通过 |
| Markdown / Entity / Controller | 54 / 60 / 40，与源码一致 |
| PowerShell 版本守卫 | 3 个项目脚本均要求 7.6+ |
| CodeGraph | 同步 6 个变更文件后为 1,185 files / 20,746 nodes / 35,554 edges，索引最新 |
| TODO/FIXME/HACK | 仅剩短信/邮件相关 4 处，占位经用户确认保留 |

## 六、供应链安全结果

### 6.1 Web

Web 生产依赖审计最终为 0。修复没有通过隐藏告警或调高阈值实现：

- 对无修复版本的 `wangeditor`，直接从生产依赖树移除，以安全禁用包满足上游表单设计器接口。
- 对 `uuid` 使用有补丁版本 `11.1.1`，并完成完整功能构建回归。
- 既有 `brace-expansion`、`ini`、`js-cookie`、`nanoid`、`postcss` override 保持生效。

### 6.2 App

App 剩余 24 条公告集中于当前 DCloud/uni-app 固定工具链，包括 `@intlify/*`、`vue-i18n`、Vite/esbuild、`adm-zip`、`phin` 和 `@babel/core`。已实际验证单独升级 Vite 或 vue-i18n 会破坏精确 peer 约束或 H5 构建，因此按用户决定不做局部强制覆盖。

后续只能升级整套 `@dcloudio/*` 后，同步升级 Vite、vue-i18n 和关联构建依赖，再重新执行 H5、微信小程序和 App 基座验收。

### 6.3 后端

- 已增加 OWASP Dependency-Check 12.2.2。
- Maven profile：`security-audit`。
- 门禁：发现 CVSS >= 7 的依赖漏洞时构建失败。
- 报告：HTML、JSON、SARIF。
- CI：支持 `NVD_API_KEY` Secret，并缓存 Dependency-Check 数据库。

本机首次执行时，NVD 返回需要同步 374,384 条记录；无 API Key 下载到 10,000 条（3%）后按用户“需要手工执行的先跳过”停止。本报告不声称后端 Maven 依赖为零漏洞。配置 Key 后执行：

```powershell
.\mvnw.cmd -Psecurity-audit verify -DskipTests "-DnvdApiKey=$env:NVD_API_KEY"
```

## 七、运行态检查

本轮实际启动并验证：

1. 后端可执行 JAR 使用 dev profile 在 HTTPS 4004 启动。
2. HikariCP 成功建立 PostgreSQL 连接。
3. Redis health 为 UP，且 Redis 配置日志不含密码或完整连接 URL。
4. Actuator health 返回 200。
5. Web 5173 登录页可渲染。
6. App H5 5174 登录页、验证码和按钮可渲染。
7. 合成刷新令牌请求触发审计写入，原始值未进入日志。
8. 验证结束后停止后端、Web 和 App，4004/5173/5174 不再监听。

本轮没有使用真实用户凭据执行登录，没有调用收费或有外部副作用的 AI Provider、短信、邮件、对象存储上传和生产第三方接口。这些属于业务验收，不影响当前环境、构建、测试和基础运行态结论。

## 八、已接受和非阻断项

| 项目 | 级别 | 处理决定 |
|---|---|---|
| App DCloud 固定依赖 24 条公告 | 已接受风险 | 等整套 uni-app/DCloud 工具链升级，不做局部升级 |
| 短信/邮件 4 处 TODO | 功能占位 | 用户确认暂不处理 |
| EmbedPDF `pdf-snippet` 约 2.08 MB | P3 性能提示 | 已按路由动态隔离；属于 PDF 引擎单模块，不通过提高警戒线隐藏 |
| EmbedPDF 浏览器构建 externalize `crypto` | 上游构建提示 | 生产构建和浏览器冒烟均通过，当前无运行错误 |
| OWASP 首次 NVD 同步 | 人工操作 | 配置 NVD API Key 后复跑；CI 已有缓存和 Key 支持 |
| Mockito 动态 Agent 提示 | 未来兼容提示 | 当前 Java 25 测试通过；后续 JDK 禁止动态 attach 前改为显式 Agent |
| 测试切片中的重复 Mapper 提示 | 测试上下文提示 | 不影响生产启动和全量测试结果 |
| vue-i18n tree-shaking 提示 | DCloud 工具链提示 | 随未来整套 DCloud 升级处理 |
| Codex 项目插件活动缓存 | 人工刷新 | 插件源码已移除 Graphify；用户环境重装插件并新建任务后刷新技能列表 |

## 九、Git 与安全检查

- 根仓库 `master` 在检测开始时领先远端 5 个既有提交，本轮不改写历史。
- 四个子项目已分别提交：
  - `spectra-admin`：`4fcad46b` `fix(admin): 加固审计日志并完善安全检测`
  - `spectra-ui`：`ea96edfa` `fix(ui): 隔离不安全富文本并优化构建拆包`
  - `spectra-app`：`5c574a33` `fix(app): 完善跨平台启动与错误处理`
  - `logicflow-plugin-flowable`：`b3aef79b` `refactor(flowable): 收敛流程模型类型边界`
- 根仓库最后提交 submodule gitlink、环境脚本、插件配置、文档和本报告。
- 提交使用 Conventional Commits 中文消息，只暂存明确文件。
- 提交前执行每个仓库的 `git diff --check`、敏感信息扫描、staged diff 复核。
- `.mise.local.toml`、数据库密码、Token、私钥、本机凭据和构建目录不得纳入提交。
- 不执行 push。

## 十、后续建议

1. 在本机或 CI 设置 `NVD_API_KEY`，完成一次后端 OWASP 全量扫描并处理真实报告。
2. DCloud 发布可兼容的新整套工具链时建立独立升级分支，完成 H5、微信小程序和 App 三端回归。
3. 确定短信和邮件供应商后实现现有 4 处发送占位及验证码闭环。
4. 使用测试账号继续做登录、权限、文件上传、工作流发布、AI Provider 等有状态业务验收。
5. 在用户终端重装 Spectra Codex 项目插件并新建任务，确认活动技能列表不再包含 Graphify。

综合判定：环境可用、框架符合预期、要求修复的问题已闭环；可在完成 Git 提交后进入下一阶段开发。
